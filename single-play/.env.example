# --- Play gate (single-play) — all optional ---

# How long a game stays "used" on a device after it's opened (hours). Default 24.
# COOLDOWN_HOURS=24

# Turn the cooldown gate off entirely (handy for local dev).
# GATE_DISABLED=1
