import TbGame from "@/components/tb/TbGame";

// ---------------------------------------------------------------------------
// /tb — टीबी का सफ़र, the TB treatment decision game for tb.care, in Hindi.
// The same game in English is at /tb/en, and /tb/hi is an alias for this page
// so the two languages can be linked to symmetrically.
// ---------------------------------------------------------------------------
export default function TbPage() {
  return <TbGame lang="hi" />;
}
