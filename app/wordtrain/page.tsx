import WordTrainGame from "@/components/wordtrain/WordTrainGame";

// The Hindi Word Train game lives at /wordtrain. No login, no backend.
export default function WordTrainPage() {
  return <WordTrainGame />;
}
